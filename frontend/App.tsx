import React, { useEffect, useState } from 'react';
import { Navigate, Route, Routes, useNavigate } from 'react-router-dom';
import { Icons } from './constants';
import { useTheme } from './contexts/ThemeContext';
import Sidebar from './components/Sidebar';
import InboxConversations from './components/InboxConversations';
import AppHeader from './components/AppHeader';
import AppErrorBoundary from './components/AppErrorBoundary';
import { AuthState, UserRole } from './types';
import { AuthService } from './services/authService';
import { apiFetch } from './services/api';

const Dashboard = React.lazy(() => import('./components/Dashboard'));
const UserManagement = React.lazy(() => import('./components/Admin/UserManagement'));
const TicketTypeManagement = React.lazy(() => import('./components/Admin/TicketTypeManagement'));
const QuickReplyManagement = React.lazy(() => import('./components/Admin/QuickReplyManagement'));
const BusinessAccessManagement = React.lazy(() => import('./components/Admin/BusinessAccessManagement'));
const AgentsManagement = React.lazy(() => import('./components/Admin/AgentsManagement'));
const MenuSettings = React.lazy(() => import('./components/Admin/MenuSettings'));
const Contacts = React.lazy(() => import('./components/Contacts'));
const Painel = React.lazy(() => import('./components/Painel'));
const BusinessAnalytics = React.lazy(() => import('./components/BusinessAnalytics'));
const BusinessAudit = React.lazy(() => import('./components/BusinessAudit'));

type WhatsappStatus = 'idle' | 'initializing' | 'qr' | 'ready' | 'disconnected' | 'error';

function LoadingScreen() {
  return <div className="app-grid flex min-h-screen items-center justify-center bg-slate-50 text-slate-600 dark:bg-slate-950 dark:text-slate-300"><div className="w-full max-w-sm space-y-4 px-6"><div className="h-12 w-12 rounded-2xl skeleton" /><div className="h-4 w-40 rounded skeleton" /><div className="h-3 w-64 rounded skeleton" /></div></div>;
}

function LoginScreen({ onSubmit, email, password, setEmail, setPassword, loading, error }: { onSubmit: (event: React.FormEvent) => void; email: string; password: string; setEmail: (value: string) => void; setPassword: (value: string) => void; loading: boolean; error: string }) {
  const [showPassword, setShowPassword] = useState(false);
  return <div className="min-h-screen grid bg-slate-50 transition-colors dark:bg-slate-950 lg:grid-cols-[1.05fr_.95fr]"><section className="app-grid relative hidden overflow-hidden bg-slate-950 p-14 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-32 -top-32 h-96 w-96 rounded-full bg-blue-500/20 blur-3xl" /><div className="relative flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-blue-600 shadow-xl shadow-blue-500/30"><Icons.Inbox className="h-6 w-6" /></div><div><p className="text-xl font-black tracking-tight">Mavo Talk</p><p className="text-[10px] uppercase tracking-[.2em] text-slate-400">Central de atendimento</p></div></div><div className="relative max-w-lg"><span className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.18em] text-blue-300"><span className="h-2 w-2 rounded-full bg-emerald-400" /> Operação em tempo real</span><h1 className="mt-5 text-5xl font-black leading-[1.05] tracking-tight">Converse melhor.<br /><span className="text-blue-400">Resolva mais rápido.</span></h1><p className="mt-6 leading-7 text-slate-400">Uma visão única para sua equipe atender clientes, organizar filas e transformar cada conversa em uma experiência melhor.</p><div className="mt-10 grid grid-cols-3 gap-3"><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><strong className="text-2xl">24/7</strong><span className="mt-1 block text-xs text-slate-400">Histórico seguro</span></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><strong className="text-2xl">1 tela</strong><span className="mt-1 block text-xs text-slate-400">Toda operação</span></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><strong className="text-2xl">IA</strong><span className="mt-1 block text-xs text-slate-400">Apoio ao time</span></div></div></div><p className="relative text-xs text-slate-500">Mavo Talk · Atendimento e negócio</p></section><section className="flex items-center justify-center px-5 py-10 sm:px-10"><div className="w-full max-w-md rounded-[28px] border border-slate-200 bg-white p-7 shadow-xl shadow-slate-200/60 dark:border-slate-800 dark:bg-slate-900 dark:shadow-black/20 sm:p-10"><div className="mb-8 flex items-center gap-3 lg:hidden"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-blue-600 text-white"><Icons.Inbox className="h-5 w-5" /></div><span className="text-xl font-black text-slate-900 dark:text-white">Mavo Talk</span></div><div className="mb-8"><p className="text-sm font-bold text-blue-600 dark:text-blue-400">Bem-vindo de volta</p><h2 className="mt-1 text-3xl font-black tracking-tight text-slate-900 dark:text-white">Acesse seu painel</h2><p className="mt-2 text-sm leading-6 text-slate-500 dark:text-slate-400">Entre para acompanhar as conversas e cuidar da operação.</p></div><form onSubmit={onSubmit} className="space-y-5"><label className="block"><span className="mb-2 ml-1 block text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400">E-mail corporativo</span><input type="email" required value={email} onChange={(event) => setEmail(event.target.value)} className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3.5 text-slate-900 outline-none transition focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10 dark:border-slate-700 dark:bg-slate-950 dark:text-white" placeholder="nome@empresa.com" autoComplete="username" /></label><label className="block"><span className="mb-2 ml-1 block text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400">Senha</span><div className="relative"><input type={showPassword ? 'text' : 'password'} required value={password} onChange={(event) => setPassword(event.target.value)} className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3.5 pr-20 text-slate-900 outline-none transition focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10 dark:border-slate-700 dark:bg-slate-950 dark:text-white" placeholder="Sua senha" autoComplete="current-password" /><button type="button" onClick={() => setShowPassword((value) => !value)} className="absolute right-3 top-1/2 -translate-y-1/2 rounded-lg p-2 text-xs font-bold text-slate-400 hover:bg-slate-200 dark:hover:bg-slate-800">{showPassword ? 'Ocultar' : 'Mostrar'}</button></div></label>{error && <div role="alert" className="rounded-xl border border-rose-200 bg-rose-50 p-3.5 text-sm font-semibold text-rose-700 dark:border-rose-900/60 dark:bg-rose-950/30 dark:text-rose-300">{error}</div>}<button type="submit" disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-blue-600 py-3.5 text-sm font-bold text-white shadow-lg shadow-blue-600/20 transition hover:-translate-y-0.5 hover:bg-blue-700 disabled:cursor-wait disabled:opacity-60">{loading && <span className="h-4 w-4 animate-spin rounded-full border-2 border-white/40 border-t-white" />}{loading ? 'Autenticando...' : 'Entrar no painel'}</button></form></div></section></div>;
}

const App: React.FC = () => {
  const navigate = useNavigate();
  const { theme, toggleTheme } = useTheme();
  const [session, setSession] = useState<AuthState | null>(AuthService.getSession());
  const [authChecked, setAuthChecked] = useState(false);
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [loginEmail, setLoginEmail] = useState('');
  const [loginPass, setLoginPass] = useState('');
  const [isLoggingIn, setIsLoggingIn] = useState(false);
  const [loginError, setLoginError] = useState('');
  const [whatsappStatus, setWhatsappStatus] = useState<WhatsappStatus | null>(null);
  const [whatsappProvider, setWhatsappProvider] = useState('');

  useEffect(() => { AuthService.refreshSession().then(setSession).catch(() => setSession(null)).finally(() => setAuthChecked(true)); }, []);
  useEffect(() => { const handleExpired = () => { void AuthService.logout(); setSession(null); navigate('/', { replace: true }); }; window.addEventListener('mavo:session-expired', handleExpired); return () => window.removeEventListener('mavo:session-expired', handleExpired); }, [navigate]);
  useEffect(() => { if (!session?.isAuthenticated || session.user?.role === UserRole.AGENT) return; const load = async () => { try { const response = await apiFetch('/api/whatsapp/status'); const data = await response.json(); if (response.ok) { setWhatsappStatus(data?.state?.status ?? null); setWhatsappProvider(data?.provider || ''); } } catch { setWhatsappStatus(null); } }; void load(); const interval = window.setInterval(() => void load(), 10000); return () => window.clearInterval(interval); }, [session?.isAuthenticated, session?.user?.role]);

  const login = async (event: React.FormEvent) => { event.preventDefault(); setIsLoggingIn(true); setLoginError(''); try { const next = await AuthService.login(loginEmail, loginPass); setSession(next); navigate('/inbox', { replace: true }); } catch (reason) { setLoginError(reason instanceof Error ? reason.message : 'Erro ao autenticar.'); } finally { setIsLoggingIn(false); } };
  const logout = async () => { await AuthService.logout(); setSession(null); navigate('/', { replace: true }); };

  if (!authChecked) return <LoadingScreen />;
  if (!session?.isAuthenticated || !session.user) return <LoginScreen onSubmit={login} email={loginEmail} password={loginPass} setEmail={setLoginEmail} setPassword={setLoginPass} loading={isLoggingIn} error={loginError} />;

  return <div className="flex h-screen min-w-0 overflow-hidden bg-slate-100 transition-colors dark:bg-slate-950">{sidebarOpen && <button type="button" aria-label="Fechar menu" onClick={() => setSidebarOpen(false)} className="fixed inset-0 z-50 bg-slate-950/50 backdrop-blur-sm md:hidden" />}<Sidebar user={session.user} mobileOpen={sidebarOpen} onNavigate={() => setSidebarOpen(false)} /><main className="relative z-0 flex min-w-0 flex-1 flex-col overflow-hidden bg-slate-50 dark:bg-slate-800/95"><AppHeader user={session.user} theme={theme} onToggleTheme={toggleTheme} onOpenMenu={() => setSidebarOpen(true)} onLogout={logout} whatsappStatus={whatsappStatus} whatsappProvider={whatsappProvider} /><AppErrorBoundary><React.Suspense fallback={<div className="flex flex-1 items-center justify-center p-8"><div className="w-full max-w-xl space-y-4"><div className="h-7 w-52 rounded-lg skeleton" /><div className="h-36 rounded-2xl skeleton" /><div className="h-36 rounded-2xl skeleton" /></div></div>}><Routes><Route path="/" element={<Navigate to="/inbox" replace />} /><Route path="/inbox" element={<InboxConversations currentUser={session.user} />} /><Route path="/dashboard" element={session.user.role === UserRole.AGENT ? <Navigate to="/inbox" replace /> : <Dashboard />} /><Route path="/business" element={session.user.role === UserRole.AGENT ? <Navigate to="/inbox" replace /> : <BusinessAnalytics />} /><Route path="/business/sincronizacao" element={session.user.role === UserRole.ADMIN ? <AgentsManagement /> : <Navigate to="/business" replace />} /><Route path="/business/auditoria" element={session.user.role === UserRole.ADMIN ? <BusinessAudit /> : <Navigate to="/business" replace />} /><Route path="/contacts" element={<Contacts />} /><Route path="/admin/usuarios" element={session.user.role === UserRole.ADMIN ? <UserManagement /> : <Navigate to="/inbox" replace />} /><Route path="/admin/tipos" element={session.user.role === UserRole.ADMIN ? <TicketTypeManagement /> : <Navigate to="/inbox" replace />} /><Route path="/admin/respostas-rapidas" element={session.user.role === UserRole.ADMIN ? <QuickReplyManagement /> : <Navigate to="/inbox" replace />} /><Route path="/admin/acessos-gerenciais" element={session.user.role === UserRole.ADMIN ? <BusinessAccessManagement /> : <Navigate to="/inbox" replace />} /><Route path="/admin/agentes" element={<Navigate to="/business/sincronizacao" replace />} /><Route path="/admin/menu-visibilidade" element={session.user.role === UserRole.ADMIN ? <MenuSettings /> : <Navigate to="/inbox" replace />} /><Route path="/painel" element={session.user.role === UserRole.AGENT ? <Navigate to="/inbox" replace /> : <Painel />} /><Route path="*" element={<Navigate to="/inbox" replace />} /></Routes></React.Suspense></AppErrorBoundary></main></div>;
};

export default App;
